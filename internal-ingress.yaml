apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ilb-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "gce-internal"
spec:
  defaultBackend:
    service:
      name: sd-webui-service2
      port:
        number: 80